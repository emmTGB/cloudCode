#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int deleteAndEarn(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        int tmp = -1;
        int first = 0, second = 0;
        for(int n : nums){
            if(tmp != n){
                tmp = n;
                second = max
                first = second;
            }
        }
    }
};